<UserControl x:Class="ST4GR3_InfusionControlApplication.Views.MenuWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ST4GR3_InfusionControlApplication.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="1.8*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" Content="Opret Infusion" Command="{Binding MenuCreateInfusion}" FontSize="20" Margin="30 50 30 120"></Button>
        <Button Grid.Row="1" Grid.Column="0" Content="Log Ud" Command="{Binding MenuLogOutCommand}" FontSize="14" Margin="60 160 60 30" Background="LightPink"></Button>

        <ListView Grid.Row="0" Grid.Column="1" Margin="10 50 30 10"></ListView>
        <ListView Grid.Row="1" Grid.Column="1" Margin="10 30 30 30"></ListView>

    </Grid>
</UserControl>
